import styled from "styled-components";

export default styled.main`
  margin-top: calc(var(--header-height) + var(--spacing-1));
  display: flex;
  flex-direction: column;
  gap: var(--spacing-1);
  padding-bottom: var(--spacing-1);
`;
